return {
  'ggandor/leap.nvim',
  -- init = function()
  -- 	require("leap").add_default_mappings()
  -- end,
  dependencies = {
    'tpope/vim-repeat',
  },

  opts = {
    add_default_mappings = true,
  },

  lazy = false,
}
